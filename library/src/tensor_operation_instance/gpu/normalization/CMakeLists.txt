set(DEV_NORM_INSTANCES)
if(DTYPES MATCHES "fp32" OR NOT DEFINED DTYPES)
    list(APPEND DEV_NORM_INSTANCES device_layernorm2d_f32_instance.cpp)
    list(APPEND DEV_NORM_INSTANCES device_layernorm4d_f32_instance.cpp)
    list(APPEND DEV_NORM_INSTANCES device_groupnorm_f32_instance.cpp)
    list(APPEND DEV_NORM_INSTANCES device_groupnorm_swish_f32_instance.cpp)
endif()
if(DTYPES MATCHES "fp16" OR NOT DEFINED DTYPES)
    list(APPEND DEV_NORM_INSTANCES device_layernorm2d_f16_instance.cpp)
    list(APPEND DEV_NORM_INSTANCES device_layernorm4d_f16_instance.cpp)
    list(APPEND DEV_NORM_INSTANCES device_groupnorm_f16_instance.cpp)
    list(APPEND DEV_NORM_INSTANCES device_groupnorm_swish_f16_instance.cpp)
endif()
if(DTYPES MATCHES "fp16" OR DTYPES MATCHES "fp32" OR NOT DEFINED DTYPES)
    list(APPEND DEV_NORM_INSTANCES device_groupnorm_swish_f16_f32_f32_f16_instance.cpp)
endif()

add_instance_library(device_normalization_instance ${DEV_NORM_INSTANCES})
